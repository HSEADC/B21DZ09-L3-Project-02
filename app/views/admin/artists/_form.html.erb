<%= form_with(model: [:admin, artist]) do |form| %>
  <% if artist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
        <% artist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :post_image, style: "display: block", value: "Обложка" %>
    <%= form.file_field :post_image %>
  </div>

  <div>
    <%= form.label :name, style: "display: block", value: "Имя" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block", value: "Описание" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit value: "ОК" %>
  </div>
<% end %>
