<%= form_for([:admin, @artist, @issue, @post, @post.comments.build]) do |f| %>
	<div class="field">
		<%= f.text_area :body, class: "comment-input", placeholder: "Leave a comment" %>
	</div>

	<div>
		<%= f.submit class: "create-button" %>
	</div>
<% end %>