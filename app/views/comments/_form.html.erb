<%= form_for([@artist, @issue, @post, @post.comments.build], url: url) do |f| %>
	<div class="field">
		<%= f.text_area :body, class: "comment-input", placeholder: "Комментарий" %>
	</div>

	<div>
		<%= f.submit class: "create-button", value: "Отправить"%>
	</div>
<% end %>